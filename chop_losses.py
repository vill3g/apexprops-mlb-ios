import json, pandas as pd; trades = json.load(open('backend/data/trades_history.json')); df = pd.DataFrame(trades); chop = df[df['trade_source'] == 'AUTO (CHOP)']; print(chop[['timestamp', 'direction', 'strike', 'pnl', 'result', 'loss_analysis']].tail())
