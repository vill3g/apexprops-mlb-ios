s = open('backend/btc/data_fetcher.py', 'r', encoding='utf-8').read(); s = s.replace('return df.tail(limit).reset_index(drop=True)_1h', 'return df_1h'); s = s.replace('return df.tail(limit).reset_index(drop=True).tail(limit).reset_index(drop=True)', 'return df'); s = s.replace('return df.tail(limit).reset_index(drop=True)_cached.tail(target_candles).reset_index(drop=True)', 'return df_cached.tail(target_candles).reset_index(drop=True)'); s = s.replace('return df.tail(limit).reset_index(drop=True).tail(target_candles).reset_index(drop=True)', 'return df.tail(target_candles).reset_index(drop=True)'); open('backend/btc/data_fetcher.py', 'w', encoding='utf-8').write(s)
